<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahtiz.mansayarbah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pdf_info"></activity>
        <activity android:name=".ActivityLevel" />
        <activity android:name=".ActivityPrinc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".choiseOfDomain" />
        <activity android:name=".finalActivity" />
        <activity android:name=".CompActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>